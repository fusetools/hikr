<Page ux:Class="HomePage">
	<ScrollView>
		<StackPanel>
			<Each Items="{hikes}">
				<Button Text="{name}" Clicked="{current.navigationService.goToHike}" />
			</Each>
		</StackPanel>
	</ScrollView>
</Page>
